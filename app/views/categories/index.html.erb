

<p id="notice"><%= notice %></p>

<h1>Categories</h1>

<%= render 'index_table.html.erb' %>

<%= paginate(@categories) %>


<%= link_to 'Back to the homepage', root_path, class: 'btn btn-info' %>

<% if can? :update, Category %>

|

<%= link_to 'New Category', new_category_path, class: 'btn btn-success' %>

<% end %>
